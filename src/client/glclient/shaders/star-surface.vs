#version 460

// Inputs - Vertices
layout (location = 0) in vec3 vPosition;
layout (location = 1) in vec3 vNornal;
layout (location = 2) in vec4 vColor;
layout (location = 3) in vec2 vTexCoords;

out vec3 position;
out vec3 normal;
out vec4 color;
out vec2 texCoords;

uniform float uTime;

int main()
{

}